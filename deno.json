{
  "name": "@andyb/env",
  "version": "0.0.2-dev.0",
  "exports": "./src/mod.ts",
  "tasks": {
    "init": "deno run -A npm:husky",
    "lint-staged": "deno run -A npm:lint-staged",
    "release": "deno run -A scripts/release.ts"
  },
  "publish": {
    "include": ["src/**/*", "LICENSE.md", "README.md"],
    "exclude": ["src/**/*.test.*"]
  },
  "lint": {
    "rules": {
      "include": ["no-console"]
    }
  }
}
